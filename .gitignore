.idea/
node_modules/
.react-router
.firebase
dist
coverage
compiled
public
docs
.DS_Store
.swp
.env.local
.env.prod
.env.developer
.divshot-cache/
*.iml
*.log
tests_output
webpack-stats.json
vendor-manifest.json
mongodb/*
tests_screenshots
aws
.env
.env.dev
.env.production
.cache
.yarn/cache
/junit.xml
/.env.development
.npmrc

# Service account credentials - never commit these
*adminsdk*.json
*service-account*.json
*serviceAccount*.json

# Local Netlify folder
.netlify

.yarn/cache
.yarn/install-state.gz
.vercel

# Raw FPL/Sheets captures — 57MB, and irreplaceable: a finished season's per-gameweek
# history cannot be re-fetched from the FPL API. Kept locally, never committed.
# The harness reads the small tracked slice in test-fixtures/ instead; regenerate it with
# `node scripts/extract-harness-stats.mjs`. See archive/README.md.
/archive/

# Live Firestore backups from scripts/backup-division-teams.mjs. Real league data, and
# the whole point of them is to exist before a destructive regeneration. Never committed.
/backups/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
