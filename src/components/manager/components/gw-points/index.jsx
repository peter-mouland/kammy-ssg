/* eslint-disable react/prop-types */
import React from 'react';

import SquadOnPitch from '../squad-on-pitch';

const GwPoints = ({ currentTeam }) => <SquadOnPitch squad={currentTeam} />;

export default GwPoints;
