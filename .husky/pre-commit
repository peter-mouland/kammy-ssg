# Formats and auto-fixes only the files you staged, then runs the tests (~2s).
# The slower checks -- type errors and the CSS convention backlog -- run in CI via
# `yarn ratchet`, so they do not sit between you and a commit.
yarn lint-staged
yarn test
